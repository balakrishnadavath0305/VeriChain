{
  "name": "verichain-blockchain",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "hardhat test",
    "deploy:amoy": "hardhat run scripts/deploy.js --network amoy"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.3"
  }
}
